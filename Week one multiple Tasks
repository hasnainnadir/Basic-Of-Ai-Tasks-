# ============================================================
#                WEEK 1 PYTHON TASKS
# ============================================================


# ============================================================
# Task 1: Reverse a String (Without Using Built-in Reverse)
# ============================================================

def reverse_string(text):
    reversed_text = ""

    for char in text:
        reversed_text = char + reversed_text

    return reversed_text


word = input("Task 1 - Enter a string: ")
print("Reversed String:", reverse_string(word))


# ============================================================
# Task 2: Check if Two Strings are Anagrams
# ============================================================

str1 = input("\nTask 2 - Enter first string: ")
str2 = input("Enter second string: ")

if sorted(str1.lower()) == sorted(str2.lower()):
    print("They are anagrams.")
else:
    print("They are not anagrams.")


# ============================================================
# Task 3: Find the Second Largest Number in a List
# ============================================================

def second_largest(numbers):
    unique_numbers = list(set(numbers))
    unique_numbers.sort()
    return unique_numbers[-2]


numbers = [10, 5, 8, 20, 15]
print("\nTask 3")
print("Numbers:", numbers)
print("Second Largest:", second_largest(numbers))


# ============================================================
# Task 4: Student Class with Grade
# ============================================================

class Student:

    def __init__(self, name, marks):
        self.name = name
        self.marks = marks

    def grade(self):
        if self.marks >= 90:
            return "A"
        elif self.marks >= 80:
            return "B"
        elif self.marks >= 70:
            return "C"
        elif self.marks >= 60:
            return "D"
        else:
            return "F"


student = Student("Ali", 85)

print("\nTask 4")
print("Name :", student.name)
print("Marks:", student.marks)
print("Grade:", student.grade())


# ============================================================
# Task 5: Exception Handling (Division by Zero & Invalid Input)
# ============================================================

print("\nTask 5")

try:
    num1 = int(input("Enter first number: "))
    num2 = int(input("Enter second number: "))

    result = num1 / num2
    print("Result:", result)

except ZeroDivisionError:
    print("Error: Cannot divide by zero.")

except ValueError:
    print("Error: Please enter valid integer numbers.")


# ============================================================
# Task 6: Simple To-Do List Application
# ============================================================

tasks = []


def add_task():
    task = input("Enter task: ")
    tasks.append(task)
    print("Task added successfully.")


def remove_task():
    task = input("Enter task to remove: ")

    if task in tasks:
        tasks.remove(task)
        print("Task removed successfully.")
    else:
        print("Task not found.")


def view_tasks():

    if len(tasks) == 0:
        print("No tasks available.")
    else:
        print("\n----- To-Do List -----")
        for i, task in enumerate(tasks, start=1):
            print(f"{i}. {task}")


print("\nTask 6 - To-Do List")

while True:

    print("\n===== MENU =====")
    print("1. Add Task")
    print("2. Remove Task")
    print("3. View Tasks")
    print("4. Exit")

    choice = input("Enter your choice: ")

    if choice == "1":
        add_task()

    elif choice == "2":
        remove_task()

    elif choice == "3":
        view_tasks()

    elif choice == "4":
        print("Exiting To-Do List...")
        break

    else:
        print("Invalid choice. Please try again.")
